# pkg/client is entirely generated by client-gen/lister-gen/informer-gen/applyconfiguration-gen (see hack/update-codegen.sh).
# Disable all checks here; golangci-lint already excludes generated code (see .golangci.yml exclusions.generated),
# but the standalone `staticcheck` binary (make staticcheck, CI staticcheck-action) has no such exclusion.
checks = ["-all"]
