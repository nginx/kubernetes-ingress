apiVersion: k8s.nginx.org/v1
kind: Policy
metadata:
  name: external-auth-oauth2-policy
spec:
  externalAuth:
    authURL: "http://oauth2-proxy-svc/auth"
    authSigninURL: "/oauth2/signin?rd=$scheme://$host$request_uri"
