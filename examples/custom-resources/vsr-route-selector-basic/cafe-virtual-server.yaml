apiVersion: k8s.nginx.org/v1
kind: VirtualServer
metadata:
  name: cafe
  namespace: default
spec:
  host: cafe.example.com
  tls:
    secret: cafe-secret
  routes:
  - path: ~/coffee
    routeSelector:
      matchLabels:
       app: cafe
