apiVersion: k8s.nginx.org/v1
kind: Policy
metadata:
  name: oidcnative-policy
spec:
  oidcNative:
    issuer: https://keycloak.example.com/realms/master
    clientID: nginx-plus
    clientSecret: oidcnative-secret
    scope: openid+profile+email
