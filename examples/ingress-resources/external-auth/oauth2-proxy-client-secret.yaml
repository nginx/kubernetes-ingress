apiVersion: v1
kind: Secret
metadata:
  name: oauth2-proxy-client-secret
type: Opaque
data:
  # Replace this with the base64-encoded client secret from Keycloak.
  # echo -n '<your-client-secret>' | base64
  client-secret: Y2hhbmdlLW1l
