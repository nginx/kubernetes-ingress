controller:
  kind: statefulset
  statefulset:
    volumeClaimTemplates:
    - metadata:
        name: nginx-cache
      spec:
        accessModes:
        - ReadWriteOnce
        resources:
          requests:
            storage: 5Gi
  volumeMounts:
  - name: nginx-cache
    mountPath: /var/cache/nginx