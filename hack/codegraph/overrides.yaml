# Domain-map heuristic overrides for the codegraph tool.
#
# The domain-map generator infers validator/config/test bindings by name
# matching against CRD names. When that fails (e.g. validator function names
# don't mention the CRD), add entries here.
#
# Schema (forward-compatible; current generator ignores this file but the
# format is reserved so adding entries is non-breaking):
#
# entries:
#   - crd: VirtualServer
#     validators:
#       - pkg/apis/configuration/validation.<funcName>
#     configs:
#       - internal/configs/version2.<StructName>
#     templates:
#       - internal/configs/version2/nginx.virtualserver.tmpl
#     tests:
#       - pkg/apis/configuration/validation/virtualserver_test.go

entries: []
